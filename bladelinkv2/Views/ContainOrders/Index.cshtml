@model IEnumerable<bladelinkv2.Models.ContainOrder>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_product)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID_Comm)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_product)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ID_Comm)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Cont }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Cont }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Cont })
        </td>
    </tr>
}

</table>
